# ShardIndex Project Rules

## Architecture
- src/indexer/ — 파일 스캐닝, 18-language parsers
- src/daemon/ — 백그라운드 프로세스 (state machine incomplete)
- src/mcp/ — Model Context Protocol (edit_plan + verify endpoints done)
- src/recovery/ — 저널/복구 (recovery journal done)
- src/database/ — SQLite index DB, integrity guard, dirty queue
- src/agent_cache.rs — Agent cache layer (TODO)

## Masterplan Status

### Completed
- Phase 1: Schema v2, integrity guard, dirty queue, MCP API
- Phase 2a: 18-language parsers (tree-sitter 0.25 API)
- Phase 2 Week 7: edit_plan + verify endpoints
- Recovery journal

### Remaining Tasks (in order)
1. Agent skill prompt template (prompts/shardindex_skill_v1.md)
2. **Agent cache layer** (src/agent_cache.rs) — DB CRUD exists, needs Rust impl
3. Confidence scoring for dynamic refs
4. Performance benchmark suite (benches/)
5. Background daemon state machine completion
6. Override registry UI/CLI

## Current Issues
- `cargo check` fails: `IndexDb` missing `#[derive(Debug)]` in src/database/mod.rs:97
- `src/daemon.rs:220` — unused `mut` on `shutdown_rx`

## Rule
After completing ANY task above, STOP and wait for "next".
